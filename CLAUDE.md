# CLAUDE.md

This repo defines the `skill-radar` Claude Code skill.

- Source of truth: `skills/skill-radar/SKILL.md`
- Edits to that file require corresponding pressure tests in `tests/`.
- Run tests by dispatching subagents per scenarios in `tests/*.md`.
- Do not summarize workflow in description — known drift trap.
